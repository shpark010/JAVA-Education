import java.util.Arrays;

/*
배열(Array)은 객체다
1. new를 통해 생성 (주소값)
2. heap 메모리에 생성된다(관리자원)
3. 정적배열(고정배열) : 배열은 한번 선언하면(크기가 정해지면) 변경 불가 <-> 동적(Collection : JAVA API클래스)
4. 자료구조(알고리즘) : 제어문과의 결합 -> 기조척인 자료구조


*/
public class Ex01_Array {
	public static void main(String[] args) {
		int s, s1, s2, s3, s4;
		int s5; int s6; int s77;
		//같은 타입의 변수 여러개를 선언해서 사용
		
		//배열
		int[] score = new int[5]; //heap메모리에 5개의 방을 갖는 배열을 생성하고 그 주소를 score에 할당
		//int 타입의 방을 5개 >> 어디에? : heap 메모리 >> 연속된 공간 >> 방이름 제공(index(점자)) >> [0][1][2][3][4]
		System.out.println(score[0]);
		score[0] = 100;
		score[1] = 200;
		score[2] = 300;
		score[3] = 400;
		score[4] = 500;
		//score[5] = 600;	
		// java.lang.ArrayIndexOutOfBoundsException: Index 5 out of bounds for length 5
		//방의 개수는 항상 index 값도바 1이 크다 (n - 1 마지막 index)
		System.out.println(score[3]);
		for (int i = 0 ; i < 5 ; i++) {
			System.out.printf("[%d] = %d", i,score[i]);
		}
		System.out.println();
		
		//배열은 객체다.
		for (int i = 0 ; i < score.length ; i++) {	//배열명.length : Array 배열의 개수를 출력
			System.out.printf("[%d] = %d", i,score[i]);
		}
		System.out.println();
		String resultArray = Arrays.toString(score);
		System.out.println(resultArray);
		
		//Tip) 당분간 쓰지 마세요.
		//배열 정렬
		//Arrays.sort(score);
		//변수 3개 자리이동 순서 바꾸는 swap or switching 하는 방법을 알고 sort를 써야한다.
		
		//정렬 알고리즘 (신입 : 버블, 퀵 코드 작성 가능해야 한다)
		
		//Today Point
		//배열 만드는 3가지 방법
		int[] arr = new int[5];	//기본 생성 방법
		int[] arr2 = new int[] {10,20,30,40,50};	//초기값을 통해서 배열의 개수를 정의하고 값을 할당
		int[] arr3 = {};
		//Tip) JavaScript는 1.const arr = [1,2,3,4,5] / 2. let arr[100,200];
		
	}

}
