/*
배열(Array)은 객체다
1. new를 통해 생성 (주소값)
2. heap 메모리에 생성된다(관리자원)
3. 정적배열(고정배열) : 배열은 한번 선언하면(크기가 정해지면) 변경 불가 <-> 동적(Collection : JAVA API클래스)
4. 자료구조(알고리즘) : 제어문과의 결합 -> 기조척인 자료구조


*/
public class Ex01_Array {
	public static void main(String[] args) {
		int s, s1, s2, s3, s4;
		int s5; int s6; int s77;
		//같은 타입의 변수 여러개를 선언해서 사용
		
		//배열
		int[] score = new int[5]; //heap메모리에 5개의 방을 갖는 배열을 생성하고 그 주소를 score에 할당
		//int 타입의 방을 5개 >> 어디에? : heap 메모리 >> 연속된 공간 >> 방이름 제공(index(점자)) >> [0][1][2][3][4]
		System.out.println(score[0]);
		score[0] = 100;
		score[1] = 200;
		score[2] = 300;
		score[3] = 400;
		score[4] = 500;
		//score[5] = 600;	
		// java.lang.ArrayIndexOutOfBoundsException: Index 5 out of bounds for length 5
		//방의 개수는 항상 index 값도바 1이 크다 (n - 1 마지막 index)
		System.out.println(score[3]);
		for (int i = 0; i < score.length; i++) {
			System.out.printf("[%d] = %d", i,score[i]);
		}
	}

}
